{
  "name": "test_runner",
  "version": "0.0.5",
  "description": "Executes Playwright-generated Python scripts as a test suite and returns an HTML report.",
  "component": "agent",
  "config": {
    "has_test_suite": true,
    "triggers": [
      {
        "name": "test_suite_tasks",
        "type": "select",
        "required": true,
        "description": "A test-suite driven trigger that supplies test_suite_id and the ordered recording files to execute.",
        "friendly_name": "Test Suite Tasks",
        "children": [
          {
            "name": "test_suite_name",
            "type": "string",
            "description": "Provide test suite name.",
            "friendly_name": "Test suite name",
            "required": true
          },
          {
            "name": "select_recordings",
            "type": "select",
            "is_multiple": true,
            "required": true,
            "description": "Select recording scripts to include in the test suite.",
            "friendly_name": "Select recordings"
          },
          {
            "name": "files",
            "type": "file",
            "required": false,
            "description": "Optional replacement Playwright script files.",
            "friendly_name": "Upload Script Files",
            "accepts": ".py"
          },
          {
            "name": "resume_from_run_id",
            "type": "string",
            "field": "text",
            "required": false,
            "description": "Sequential mode only — paste a previous run ID to resume from its first failed recording. Leave empty to start fresh.",
            "friendly_name": "Resume from run ID"
          },
          {
            "name": "after_action_wait_ms",
            "type": "number",
            "required": false,
            "description": "Milliseconds to wait after each action (settle time) before capturing the step. Leave empty to use the runner default. A per-recording value overrides this suite default.",
            "friendly_name": "After-action wait (ms)"
          }
        ]
      }
    ],
    "list_agent": true
  }
}
