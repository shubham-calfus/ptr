# Codex Custom Instructions

Paste the block below into Codex Custom Instructions if you want Codex to stay
aligned with the Oracle runner redesign.

```text
This repository has two separate systems:

1. playwright-codegen
- This is the recording/code generation side.
- Phantom owns the real Playwright desktop session, noVNC/browser lifecycle, and raw generated recording scripts.

2. playwright_test_runner
- This is the execution side.
- The runner downloads stored Python Playwright recordings and executes them directly in its own worker process.
- The runner does not execute recordings through Phantom.

Project rule:
- Once a Playwright script is generated by Phantom and handed to the runner, treat it as read-only execution input.
- Do not solve runner failures by editing or rewriting generated scripts unless I explicitly ask.
- Phantom owns generation quality. The runner owns execution quality.

Current goal:
- Rebuild the Oracle-focused runner from scratch into a small, deterministic, debuggable system.
- Remove current broad fallback behavior and multi-direction logic.
- Do not preserve old and new execution models in parallel.
- Prefer deleting complexity over adding more heuristics.

Execution architecture to follow:
strict execution -> minimal scoped wait/delay -> Oracle-specific control handler -> experience retrieval -> AI self-repair -> postcondition validation -> fail clearly

Non-negotiable execution rules:
- Use the raw recorded Playwright locator first.
- Do not replace strict execution with broad role/text/label scanning unless strict execution fails.
- Do not add generic “try everything” fallback chains.
- Do not allow global readiness heuristics to block an action if the actual target locator is already actionable.
- Prefer action-scoped waits over global page settle gates.
- Avoid networkidle as a hard dependency unless truly required.

Oracle support requirements:
Support Oracle ADF / Redwood controls intentionally and explicitly, not generically.
Build dedicated handlers for:
- quick actions / show more quick actions
- guided process navigation / step transitions
- oj-select-single / oj-c-select-single
- oj-input-text / oj-c-input-text
- oj-input-number / oj-c-input-number
- oj-input-date / oj-c-input-date
- oj-text-area / oj-c-text-area
- LOV / listbox / popup / dropdown
- split buttons / menu panels / ADF popup menus
- action cards / switches / checkboxes
- table / row / cell selection
- dialog and popup interaction patterns

Handler design rules:
- Prefer deterministic Oracle-aware handlers over AI.
- Keep handlers narrowly scoped to known Oracle control patterns.
- If adding a recovery rule, explain exactly which Oracle pattern it is for.
- Do not introduce broad global behavior that can affect unrelated flows.

Experience system rules:
- Treat runner learning as structured experience retrieval, not chat memory.
- Store failures and recoveries as structured episodes with page signature, failure signature, recovery, postcondition, and outcome.
- Reuse only validated experience entries whose postcondition passed.
- Promote repeated successful Oracle-specific recoveries into deterministic handlers.
- Never auto-reuse a recovery that previously clicked or selected the wrong semantic target.

AI self-repair rules:
- AI self-repair is last resort only.
- AI is a recovery layer, not the primary execution path.
- Only send AI the minimum useful DOM/context needed for the failed action.
- AI-generated locators must be semantically validated against the requested label/control before use.
- Reject AI suggestions that do not clearly match the intended target.
- Never mark success just because an AI click did not throw.

Success rules:
- Every action must have a postcondition.
- A non-throwing click is not success.
- Valid postconditions include:
  - target value changed
  - expected row/cell selected
  - dialog opened
  - popup/menu opened
  - guided step changed
  - Oracle control state changed
  - URL changed
  - expected page content appeared
- If postcondition is not true, the action failed.

Debugging rules:
- Always identify which layer failed before adding code:
  1. strict locator
  2. wait/delay
  3. Oracle control handler
  4. experience retrieval
  5. AI self-repair
  6. postcondition validation
- Do not patch blindly.
- Do not broaden fallback scope unless the exact failure layer requires it.
- If a proposed change increases global complexity or regression risk, say so explicitly.

Working style:
- Optimize for correctness, determinism, and debuggability over recovery rate.
- Keep implementation in one direction only.
- Simplify instead of patching around old complexity.
- If a feature or helper causes regressions, prefer removing or replacing it rather than stacking more logic on top.
- When changing runner behavior, also update tests so the new architecture is enforced.
```
